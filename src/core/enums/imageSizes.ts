// Get this from the configuration API. Too lazy to call it so make it enum :v.
/** Backdrop size. */
export enum BackdropSizes {
  small = 'w300',
  medium = 'w780',
  large = 'w1280',
  original = 'original',
}

/** Logo sizes. */
export enum LogoSizes {
  extraSmall = 'w45',
  small = 'w92',
  medium = 'w154',
  large = 'w185',
  extraLarge = 'w300',
  extraExtraLarge = 'w500',
  original = 'original',
}

/** Poster sizes. */
export enum PosterSizes {
  extraSmall = 'w92',
  small = 'w154',
  medium = 'w185',
  large = 'w342',
  extraLarge = 'w500',
  extraExtraLarge = 'w780',
  original = 'original',
}

/** Profile sizes. */
export enum ProfileSizes {
  small = 'w45',
  medium = 'w185',
  large = 'h632',
  original = 'original',
}
